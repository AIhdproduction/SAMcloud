# Potree assets

Place the Potree viewer distribution in this directory before enabling the
interactive dense point cloud viewer. Generated point cloud viewer data belongs
in each local project under `products/viewer/` and is excluded from Git.
